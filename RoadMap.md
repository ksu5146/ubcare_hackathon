# RoadMap: 부동산 매수 도우미 포털

> **문서 버전:** v1.0
> **작성일:** 2026-03-13
> **기준 문서:** [PRD.md](./PRD.md)
> **상태:** Draft

---

## 1. 전체 로드맵 개요

```
Phase 1 (MVP)          Phase 2              Phase 3              Phase 4~6
Sprint 1~4             Sprint 5~6           Sprint 7~8           Sprint 9+
[2주 × 4 = 8주]        [2주 × 2 = 4주]      [2주 × 2 = 4주]      [지속 개선]

 프로젝트 초기 설정       전세/월세 분석        등시선 지도           AI 추천
 API 프록시 계층         전세가율 트렌드       자차 경로 분석        사용자 계정
 조건 설정 UI           갭투자 분석 지표       생활권 분석           알림 시스템
 실거래가 조회/차트       추가 부동산 유형      편의시설 카테고리      모바일 앱
 지도 + 리스트 뷰        정렬/페이지네이션      인구 현황             공유/내보내기
 단지 상세 페이지        건축년도/세대수 필터   조건 프리셋 저장
 주변 호재 (서울)        학군 상세 정보         층수 필터
 출퇴근 (대중교통)       교통 호재             공시가 비율
 관심 단지 + 비교(2개)   비교 확장(4개)        비교 리포트
```

---

## 2. Phase 1: MVP (Sprint 1~4, 8주)

### 목표
> 서울 지역 아파트 매매 실거래가를 조건별로 검색하고, 단지별 가격 추이를 확인하며, 주변 호재와 출퇴근 시간을 파악하고, 2개 단지를 비교할 수 있는 최소 기능 포털

---

### Sprint 1: 프로젝트 부트스트랩 + API 파운데이션 (2주)

#### S1 진행 현황

| 항목 | 내용 |
|------|------|
| **완료율** | 100% (12/12 태스크) |
| **소요 시간** | 약 2일 |
| **블로커/리스크** | Windows 기업 환경 SSL 인증서 오류 → `.npmrc`에 `node-options=--use-system-ca` 추가로 해결 |
| **의사결정** | shadcn/ui 대신 Radix UI 직접 사용 결정 (스타일 자유도 우선); Tailwind v4 `@theme inline` 방식 채택 (별도 config 파일 제거) |

#### S1-Goal
> 프로젝트 골격 완성, 공공데이터 API 연동 검증, 법정동코드 데이터 확보

#### S1-Task 목록

| ID | 태스크 | 상세 설명 | 산출물 | 검증 방법 | PRD 매핑 |
|----|--------|-----------|--------|-----------|----------|
| S1-01 | Next.js 프로젝트 초기화 | `create-next-app` (App Router, TypeScript, Tailwind CSS, ESLint), shadcn/ui 설치, 프로젝트 디렉토리 구조 설정 | `package.json`, `tsconfig.json`, `tailwind.config.ts`, `app/` 디렉토리 | `npm run dev` 실행 후 localhost:3000 정상 표시 | NF-1 |
| S1-02 | 디렉토리 구조 설계 | 아래 [디렉토리 구조 명세](#디렉토리-구조-명세-s1-02) 참고 | 디렉토리 트리 | 각 디렉토리 존재 확인 | - |
| S1-03 | 환경변수 설정 | `.env.local` 템플릿 작성 — `DATA_GO_KR_API_KEY`, `KAKAO_REST_API_KEY`, `KAKAO_JS_APP_KEY`, `ODSAY_API_KEY` | `.env.local.example` | 환경변수 로드 확인 (`process.env.*`) | NF-5 |
| S1-04 | API 프록시 기반 구조 구축 | Next.js Route Handler 기반 API 프록시 패턴 구현 — 공통 에러 핸들링, 타임아웃(3초), 재시도(1회), 응답 정규화 | `app/api/` 하위 라우트 핸들러 | 프록시 경유 API 호출 시 서비스키 미노출 확인 | NF-5 |
| S1-05 | 캐시 레이어 구현 | `Map` 기반 인메모리 캐시 (MVP), TTL 설정 (실거래가 24h, 단지정보 7d), 캐시 히트/미스 로깅 | `lib/cache.ts` | 동일 요청 2회 시 두 번째 응답 < 50ms | NF-6 |
| S1-06 | 법정동코드 데이터 확보 | code.go.kr에서 법정동코드 전체 다운로드 → JSON 변환, 시/도 → 시/군/구 → 읍/면/동 계층 트리 구조 생성 | `data/region-codes.json` (약 5,000개 법정동) | JSON 파싱 성공, 서울특별시 > 강남구 > 역삼동 경로 존재 확인 | F1-2 |
| S1-07 | 아파트 매매 실거래가 API 연동 | data.go.kr `15126469` 연동 — Route Handler: `GET /api/trade/apartment` — 파라미터: `lawdCd`(법정동5자리), `dealYmd`(YYYYMM) — 응답 파싱 및 TypeScript 타입 정의 | `app/api/trade/apartment/route.ts`, `types/trade.ts` | curl 테스트: 강남구(11680) 2025년 12월 데이터 최소 1건 반환 | F3-1 |
| S1-08 | 단지 목록 API 연동 | data.go.kr `15057332` 연동 — Route Handler: `GET /api/complex/list` — 법정동코드 기반 단지 검색 | `app/api/complex/list/route.ts`, `types/complex.ts` | 강남구 단지 목록 10건 이상 반환 | F2-1 |
| S1-09 | 단지 기본정보 API 연동 | data.go.kr `15058453` 연동 — Route Handler: `GET /api/complex/[id]` — 단지코드 기반 상세 정보 (세대수, 동수 등) | `app/api/complex/[id]/route.ts` | 특정 단지코드로 세대수 포함 응답 확인 | F2-2 |
| S1-10 | 공통 UI 컴포넌트 셋업 | shadcn/ui 컴포넌트 설치 — Button, Input, Select, Slider, Card, Badge, Tabs, Dialog, Skeleton | `components/ui/` | Storybook 없이 개발 페이지(`/dev`)에서 렌더링 확인 | - |
| S1-11 | 글로벌 레이아웃 구현 | 헤더(로고, 네비게이션, 관심목록 버튼), 메인 영역, 반응형 컨테이너 | `app/layout.tsx`, `components/layout/` | 모바일(360px)~데스크톱(1920px) 레이아웃 깨짐 없음 | NF-3 |
| S1-12 | API 응답 타입 통합 정의 | 실거래가, 단지정보 API 공통 응답 인터페이스 — `ApiResponse<T>`, 에러 타입, 페이지네이션 타입 | `types/api.ts`, `types/trade.ts`, `types/complex.ts` | TypeScript 빌드 에러 0건 | - |

#### 디렉토리 구조 명세 (S1-02)

```
src/
├── app/
│   ├── layout.tsx              # 글로벌 레이아웃
│   ├── page.tsx                # 메인 (조건 설정)
│   ├── search/
│   │   └── page.tsx            # 검색 결과 (지도+리스트)
│   ├── complex/
│   │   └── [id]/
│   │       └── page.tsx        # 단지 상세
│   ├── compare/
│   │   └── page.tsx            # 비교 페이지
│   └── api/
│       ├── trade/
│       │   └── apartment/
│       │       └── route.ts    # 아파트 매매 실거래가 프록시
│       ├── complex/
│       │   ├── list/
│       │   │   └── route.ts    # 단지 목록 프록시
│       │   └── [id]/
│       │       └── route.ts    # 단지 상세 프록시
│       ├── location/
│       │   └── search/
│       │       └── route.ts    # 카카오 로컬 검색 프록시
│       ├── transit/
│       │   └── route.ts        # ODsay 대중교통 프록시
│       └── nearby/
│           ├── school/
│           │   └── route.ts    # 학교 정보 프록시
│           └── redevelopment/
│               └── route.ts    # 재개발 정보 프록시
├── components/
│   ├── ui/                     # shadcn/ui 컴포넌트
│   ├── layout/                 # Header, Footer, Container
│   ├── filter/                 # 조건 설정 컴포넌트
│   ├── map/                    # 카카오맵 관련 컴포넌트
│   ├── chart/                  # Recharts 차트 컴포넌트
│   ├── complex/                # 단지 관련 컴포넌트
│   └── compare/                # 비교 관련 컴포넌트
├── lib/
│   ├── cache.ts                # 인메모리 캐시
│   ├── api-client.ts           # 내부 API 호출 유틸
│   ├── format.ts               # 금액/면적 포맷 유틸
│   └── constants.ts            # 상수 (API URL, TTL 등)
├── hooks/
│   ├── use-trade-data.ts       # 실거래가 데이터 훅
│   ├── use-complex.ts          # 단지 정보 훅
│   └── use-favorites.ts        # 관심 단지 훅
├── types/
│   ├── api.ts                  # 공통 API 타입
│   ├── trade.ts                # 실거래가 타입
│   ├── complex.ts              # 단지 정보 타입
│   └── filter.ts               # 필터 조건 타입
└── data/
    └── region-codes.json       # 법정동코드 JSON
```

#### S1 완료 기준 (Definition of Done)
- [ ] `npm run build` 에러 0건
- [ ] `npm run dev` → localhost:3000 정상 표시
- [ ] `/api/trade/apartment?lawdCd=11680&dealYmd=202512` → JSON 응답 반환
- [ ] `/api/complex/list?lawdCd=11680` → 단지 목록 JSON 반환
- [ ] 동일 API 2회 호출 시 캐시 히트 확인 (서버 로그)
- [ ] `.env.local.example` 에 모든 필요 키 문서화

---

### Sprint 2: 조건 설정 + 검색 결과 (지도/리스트) (2주)

#### S2 진행 현황

| 항목 | 내용 |
|------|------|
| **완료율** | 100% (13/13 태스크) |
| **소요 시간** | 약 2일 |
| **블로커/리스크** | 없음 |
| **의사결정** | URL searchParams 기반 필터 상태 관리 채택 — 뒤로가기/새로고침 시 필터 복원 및 URL 공유 가능성 확보; 카카오맵 마커 클러스터러 적용으로 다수 마커 성능 개선 |

#### S2-Goal
> 사용자가 예산, 지역, 면적을 설정하면 조건에 맞는 아파트 단지를 카카오맵 + 리스트로 확인할 수 있다

#### S2-Task 목록

| ID | 태스크 | 상세 설명 | 산출물 | 검증 방법 | PRD 매핑 |
|----|--------|-----------|--------|-----------|----------|
| S2-01 | 지역 선택 캐스케이드 드롭다운 | `region-codes.json` 기반 시/도 → 시/군/구 → 읍/면/동 3단계 Select, 상위 변경 시 하위 초기화, 복수 구 선택 가능 | `components/filter/RegionSelect.tsx` | 서울 > 강남구 선택 → 읍/면/동 목록 20개+ 표시 | F1-2 |
| S2-02 | 매매가 범위 슬라이더 | 듀얼 핸들 Range Slider, 범위: 0 ~ 30억, 단위: 1,000만원, 값 포맷팅 (예: "5억 ~ 7억"), 프리셋 버튼 (3억 이하, 3~5억, 5~7억, 7~10억, 10억+) | `components/filter/PriceRange.tsx` | 슬라이더 조작 시 값 실시간 반영, 프리셋 클릭 시 슬라이더 이동 | F1-1 |
| S2-03 | 전용면적 범위 슬라이더 | 듀얼 핸들 Range Slider, 범위: 10~200 m², m²↔평 전환 버튼 (1평=3.3058m²), 프리셋: 국민평형(59m²), 중형(84m²) | `components/filter/AreaRange.tsx` | 평/m² 전환 시 값 올바르게 변환 (59m² ↔ 17.8평) | F1-3 |
| S2-04 | 부동산 유형 선택 | 체크박스 그룹: 아파트(기본 선택), 연립다세대, 오피스텔, 단독다가구 — MVP에서는 아파트만 동작, 나머지 비활성 처리 + "준비중" 뱃지 | `components/filter/PropertyType.tsx` | 아파트 체크 시 검색 가능, 나머지 선택 시 "Phase 2에서 지원 예정" 툴팁 | F1-7 |
| S2-05 | 필터 상태 관리 | URL 쿼리파라미터 기반 상태 관리 (`useSearchParams`), 필터 변경 → URL 동기화 → 뒤로가기 시 필터 복원, 타입 정의: `FilterState` | `hooks/use-filter.ts`, `types/filter.ts` | URL에 `?lawdCd=11680&priceMin=50000&priceMax=70000&areaMin=59&areaMax=84` 반영 확인, 새로고침 후 필터 유지 | F1-1~7 |
| S2-06 | 조건 설정 메인 페이지 | 필터 컴포넌트 조합, "매물 검색" 버튼 → `/search?...` 라우팅, 반응형: 모바일 세로 배치, 데스크톱 그리드 | `app/page.tsx` | 모바일(360px): 세로 스택, 데스크톱(1280px): 2열 그리드 | F1-* |
| S2-07 | 검색 데이터 페칭 로직 | 필터 조건 → 법정동 코드 추출 → 선택된 모든 구의 최근 12개월 실거래가 병렬 조회 → 가격/면적 조건으로 클라이언트 필터링 → 단지명 기준 그룹핑 | `hooks/use-search-results.ts`, `lib/search.ts` | 강남구 5~7억 59~84m² 검색 → 단지 목록 반환 (각 단지에 최근 거래가, 거래 건수 포함) | F2-1 |
| S2-08 | 카카오맵 통합 컴포넌트 | 카카오맵 JS SDK 동적 로드 (`next/script`), 지도 초기화/리사이즈, 마커 생성/제거, 마커 클러스터러, 인포윈도우, 지도 영역 변경 이벤트 | `components/map/KakaoMap.tsx`, `components/map/MapMarker.tsx`, `components/map/MapCluster.tsx` | 서울 강남구 중심 지도 표시, 마커 10개 이상 렌더링, 줌 아웃 시 클러스터링 | F2-1 |
| S2-09 | 검색 결과 리스트 뷰 | 카드형 리스트: 단지명, 최근 거래가 (볼드), 전용면적, 건축년도, 세대수, 소재지, 거래 건수 뱃지 — 카드 클릭 → `/complex/[id]` 이동 | `components/complex/ComplexCard.tsx`, `components/complex/ComplexList.tsx` | 검색 결과 20개 이상일 때 스크롤 가능, 카드 클릭 시 상세 페이지 이동 | F2-2 |
| S2-10 | 지도-리스트 연동 | 지도 마커 클릭 → 리스트에서 해당 카드 하이라이트 + 스크롤, 리스트 카드 호버/클릭 → 지도에서 해당 마커 강조 (바운스 애니메이션) | `app/search/page.tsx` 내 상태 연동 | 마커 클릭 시 리스트 스크롤 이동 확인, 카드 호버 시 마커 색상 변경 확인 | F2-3 |
| S2-11 | 검색 결과 페이지 레이아웃 | 좌우 분할: 좌측 지도(60%) + 우측 리스트(40%), 모바일: 탭 전환 (지도/리스트), 상단: 필터 요약 바 + "조건 수정" 버튼 + 결과 수 표시 | `app/search/page.tsx` | 데스크톱: 좌우 분할, 모바일: 탭 전환 동작 | F2-1~3 |
| S2-12 | 금액/면적 포맷 유틸리티 | `formatPrice(50000)` → "5억", `formatPrice(52000)` → "5억 2,000만", `formatArea(59.99, 'pyeong')` → "18.1평", `formatArea(59.99, 'sqm')` → "59.99m²" | `lib/format.ts` | 단위 테스트 10개 이상 통과 | - |
| S2-13 | 로딩/에러 상태 UI | Skeleton 로더 (카드, 지도 영역), 에러 발생 시 "데이터를 불러올 수 없습니다. 다시 시도해주세요." + 재시도 버튼, 빈 결과: "조건에 맞는 매물이 없습니다. 조건을 완화해보세요." | `components/ui/LoadingCard.tsx`, `components/ui/ErrorState.tsx`, `components/ui/EmptyState.tsx` | 네트워크 차단 시 에러 UI 표시, 재시도 클릭 시 재요청 | NF-2 |

#### S2 완료 기준
- [ ] 메인 페이지에서 조건 설정 후 "매물 검색" → 검색 결과 페이지 이동
- [ ] 검색 결과: 카카오맵에 마커 표시 + 우측 리스트에 단지 카드 표시
- [ ] 지도 마커 ↔ 리스트 카드 양방향 연동
- [ ] URL 쿼리파라미터 기반 필터 상태 유지 (새로고침, 뒤로가기)
- [ ] 모바일 360px에서 조건 설정, 검색 결과 모두 사용 가능
- [ ] Lighthouse Performance 70+

---

### Sprint 3: 단지 상세 + 실거래가 차트 + 출퇴근 (2주)

#### S3 진행 현황

| 항목 | 내용 |
|------|------|
| **완료율** | 100% (11/11 태스크) |
| **소요 시간** | 약 2일 |
| **블로커/리스크** | 없음 |
| **의사결정** | 5년치 실거래가 60개월 병렬 조회 시 API 호출 과다 문제 예견 → 배치 수집 전환의 필요성 확인 (Sprint 5 피벗 근거); ODsay 클라이언트 직접 호출 방식으로 전환하여 서버 프록시 오버헤드 제거 |

#### S3-Goal
> 단지 상세 페이지에서 실거래가 시계열 차트와 거래 내역 테이블을 확인하고, 직장까지 대중교통 출퇴근 시간을 조회할 수 있다

#### S3-Task 목록

| ID | 태스크 | 상세 설명 | 산출물 | 검증 방법 | PRD 매핑 |
|----|--------|-----------|--------|-----------|----------|
| S3-01 | 단지 상세 페이지 레이아웃 | 헤더 (단지명, 관심/비교 버튼), 기본 정보 섹션, 차트 섹션, 거래 내역 섹션, 주변 분석 섹션, 출퇴근 섹션 — 세로 스크롤, 섹션별 sticky 네비게이션 | `app/complex/[id]/page.tsx` | 모든 섹션 렌더링, 섹션 네비게이션 클릭 시 스크롤 이동 | F3-* |
| S3-02 | 단지 기본 정보 카드 | 주소, 건축년도, 세대수, 동수, 난방방식, 주차대수 — 단지정보 API(`15058453`) 데이터 매핑 | `components/complex/ComplexInfo.tsx` | 세대수, 건축년도 등 최소 5개 필드 표시 | F2-2 |
| S3-03 | 실거래가 데이터 집계 로직 | 단지명 + 법정동 기준으로 실거래가 히스토리 조회 (최대 5년치 = 60개월), 월별 병렬 요청 (5년 = 60 요청 → 연도별 12개 병렬 × 5 순차), 결과 캐시 | `lib/trade-aggregator.ts` | 5년치 데이터 수집 완료 후 월별 평균가, 거래건수, 최고가, 최저가 산출 | F3-1 |
| S3-04 | 실거래가 시계열 라인 차트 | Recharts `LineChart` — X축: 년/월, Y축: 거래가(만원, 억 포맷), 기간 선택 탭(1Y/3Y/5Y/전체), 툴팁(날짜, 가격, 층, 면적), 데이터 포인트 hover 시 상세 | `components/chart/PriceChart.tsx` | 차트 렌더링, 기간 전환 시 데이터 범위 변경, 툴팁 정보 표시 | F3-1 |
| S3-05 | 평형별 필터 | 단지 내 존재하는 전용면적 목록 추출 → Select 컴포넌트, 선택 시 차트 + 테이블 해당 면적만 필터링, "전체" 옵션 포함 | `components/complex/AreaFilter.tsx` | 59m², 84m² 2개 면적 존재 시 → 59m² 선택 → 차트에 59m² 거래만 표시 | F3-2 |
| S3-06 | 거래 상세 테이블 | 컬럼: 계약일, 층, 전용면적, 거래가, 건축년도 — 최신순 정렬(기본), 정렬 가능(가격순, 층순), 페이지네이션(20건/페이지) | `components/complex/TradeTable.tsx` | 테이블 렌더링, 컬럼 헤더 클릭 시 정렬 토글 | F3-4 |
| S3-07 | 카카오 로컬 주소 검색 API 연동 | Route Handler: `GET /api/location/search?query=삼성역` — 카카오 REST API 키워드 검색 → 장소명, 주소, 좌표(lat/lng) 반환 | `app/api/location/search/route.ts` | "삼성역" 검색 → 좌표 포함 결과 반환 | F5-1 |
| S3-08 | 직장 주소 검색 입력 컴포넌트 | 텍스트 입력 + 디바운스(300ms) + 자동완성 드롭다운 (카카오 로컬 API), 선택 시 좌표 저장, localStorage에 최근 검색 5개 저장 | `components/transit/WorkplaceSearch.tsx` | "삼성역" 입력 → 자동완성 5개 이상 표시 → 선택 시 좌표 설정 | F5-1 |
| S3-09 | ODsay 대중교통 경로 API 연동 | Route Handler: `GET /api/transit?sx=127.0&sy=37.5&ex=127.1&ey=37.5` — ODsay 대중교통 경로 검색 → 소요시간, 환승횟수, 요금, 경로요약 반환 | `app/api/transit/route.ts` | 강남역→삼성역 좌표 입력 → 소요시간(분) + 환승횟수 반환 | F5-2 |
| S3-10 | 출퇴근 분석 섹션 UI | 직장 검색 → 대중교통 소요시간/환승횟수/요금 표시, 아이콘 + 수치 카드형 레이아웃, 경로 요약 텍스트 (예: "2호선 → 환승 → 9호선") | `components/transit/CommuteResult.tsx` | 직장 선택 후 3초 이내 소요시간 표시 | F5-2 |
| S3-11 | 데이터 기준일 표시 | 모든 데이터 표시 영역에 "데이터 기준: YYYY.MM" 배지, 실거래가 최신 데이터 월 자동 감지 | `components/ui/DataBadge.tsx` | 각 섹션에 데이터 기준일 표시 확인 | PRD 10 (리스크) |

#### S3 완료 기준
- [ ] `/complex/[id]` → 단지 기본정보 + 실거래가 차트 + 거래 내역 테이블 표시
- [ ] 차트 기간 전환 (1Y/3Y/5Y) 정상 동작
- [ ] 평형별 필터 적용 시 차트 + 테이블 데이터 일치
- [ ] 직장 주소 검색 → 대중교통 출퇴근 시간 표시
- [ ] 5년치 실거래가 조회 p95 < 5초 (캐시 미스 시)
- [ ] 캐시 히트 시 p95 < 500ms

---

### Sprint 4: 주변 호재 + 관심 목록 + 비교 + 마감 (2주)

#### S4 진행 현황

| 항목 | 내용 |
|------|------|
| **완료율** | 부분 완료 — 관심목록(S4-06~07), 비교(S4-08~09), SEO(S4-10), 에러처리(S4-11), 성능(S4-12), 배포(S4-13) 완료. 주변 호재/학군(S4-01~05)은 mock 데이터 한계로 비활성화 처리 |
| **소요 시간** | 약 2일 |
| **블로커/리스크** | 서울열린데이터 재개발/재건축 API 응답 불안정 + NEIS 학교 좌표 데이터 부정확 → NearbyMap 컴포넌트 구현 완료 후 UI에서 비활성화 결정 |
| **의사결정** | 주변 분석 섹션을 `components/nearby/`에 구현하되 단지 상세 페이지에서 조건부 비활성화 처리; 실시간 API 호출 방식의 근본적 한계 확인 → Sprint 5에서 DB 사전 수집 아키텍처로 전환 결정 |

#### S4-Goal
> 단지 주변 호재/학군 표시, 관심 단지 담기, 2개 단지 비교 기능을 완성하고 MVP 출시 품질 달성

#### S4-Task 목록

| ID | 태스크 | 상세 설명 | 산출물 | 검증 방법 | PRD 매핑 |
|----|--------|-----------|--------|-----------|----------|
| S4-01 | 서울 재개발/재건축 API 연동 | Route Handler: `GET /api/nearby/redevelopment?lat=37.5&lng=127.0&radius=2000` — 서울열린데이터 `OA-2253` 연동, 반경 2km 필터링 | `app/api/nearby/redevelopment/route.ts` | 강남구 좌표 → 반경 2km 내 정비사업 목록 반환 | F4-1 |
| S4-02 | NEIS 학교 정보 API 연동 | Route Handler: `GET /api/nearby/school?lawdCd=11680` — NEIS 학교기본정보 API(`15122275`) → 초/중/고 목록 + 좌표 | `app/api/nearby/school/route.ts` | 강남구 학교 목록 반환 (학교명, 학교급, 주소) | F4-3 |
| S4-03 | 주변 분석 지도 탭 UI | 단지 상세 페이지 내 "주변 분석" 섹션 — 탭: [호재] [학군] — 카카오맵 미니맵에 카테고리별 마커 오버레이, 마커 클릭 시 인포윈도우 (이름, 거리, 상세정보) | `components/nearby/NearbyMap.tsx`, `components/nearby/NearbyTabs.tsx` | [호재] 탭: 재개발 구역 마커 표시, [학군] 탭: 학교 마커 표시, 탭 전환 시 마커 교체 | F4-1, F4-3 |
| S4-04 | 호재 정보 카드 | 정비사업 구역명, 사업유형(재개발/재건축), 추진단계(정비구역지정/조합설립/사업시행/관리처분/착공/준공), 단지로부터의 거리, 예상 세대수 | `components/nearby/RedevelopmentCard.tsx` | 카드에 사업유형, 추진단계 배지 표시 | F4-1 |
| S4-05 | 학군 정보 카드 | 학교명, 학교급(초/중/고) 아이콘, 주소, 단지로부터의 거리(직선), 설립유형(국/공/사립) | `components/nearby/SchoolCard.tsx` | 반경 1km 내 학교 카드 리스트 표시 | F4-3 |
| S4-06 | 관심 단지 기능 (localStorage) | 하트 버튼 토글 → localStorage `favorites` 키에 단지 정보 저장/삭제, 최대 20개, 초과 시 경고, 검색 결과/상세 페이지 모두에서 동작 | `hooks/use-favorites.ts`, `components/complex/FavoriteButton.tsx` | 하트 클릭 → localStorage 확인, 새로고침 후 하트 상태 유지, 21번째 추가 시 경고 | F6-1 |
| S4-07 | 관심 목록 페이지 | 헤더 "관심목록" 버튼 → 모달 또는 사이드 드로어, 저장된 단지 리스트 (카드형), 삭제 버튼, "비교하기" 버튼 (2개 선택 시 활성화) | `components/favorites/FavoritesList.tsx` | 관심 단지 3개 저장 → 목록에 3개 표시, 삭제 시 즉시 반영 | F6-1 |
| S4-08 | 단지 비교 페이지 | URL: `/compare?ids=단지1,단지2`, 나란히 비교: 기본 정보 테이블 (주소, 건축년도, 세대수), 실거래가 차트 오버레이 (2개 단지 라인 겹침, 색상 구분), 출퇴근 시간 비교 | `app/compare/page.tsx`, `components/compare/CompareView.tsx` | 2개 단지 선택 → 비교 페이지에서 차트 오버레이 + 정보 테이블 표시 | F6-2 |
| S4-09 | 비교 차트 (오버레이) | Recharts `LineChart` 2개 데이터셋, 범례(단지명 + 색상), 동일 X축(기간), 동일 Y축(가격) | `components/chart/CompareChart.tsx` | 2개 단지의 가격 추이가 하나의 차트에 색상 구분되어 표시 | F6-2 |
| S4-10 | SEO + 메타데이터 | Next.js `metadata` API — 페이지별 title/description, OpenGraph 이미지 (기본), favicon | `app/layout.tsx`, 각 페이지 `metadata` | og:title, og:description 확인 (메타 태그 검사) | - |
| S4-11 | 에러 바운더리 + 404 페이지 | 글로벌 에러 바운더리 (`error.tsx`), 404 페이지 (`not-found.tsx`), API 에러 시 사용자 친화적 메시지 | `app/error.tsx`, `app/not-found.tsx` | 존재하지 않는 단지 ID → 404 페이지 표시 | NF-2 |
| S4-12 | MVP 통합 테스트 + 성능 최적화 | 전체 사용자 플로우 테스트 (조건 설정 → 검색 → 상세 → 관심 → 비교), Lighthouse 성능 측정 + 이미지 최적화 + 번들 사이즈 점검 | 테스트 체크리스트 | Lighthouse Performance 80+, LCP < 2.5s, 전체 플로우 에러 없이 완료 | NF-1, NF-3 |
| S4-13 | Vercel 배포 설정 | Vercel 프로젝트 연결, 환경변수 설정, 도메인 설정 (선택), Preview 배포 확인 | Vercel 배포 URL | 배포 URL 접속 → 전체 기능 동작 확인 | - |

#### S4 완료 기준 (= MVP 출시 기준)
- [ ] **핵심 플로우**: 조건 설정 → 검색 결과 (지도+리스트) → 단지 상세 (차트+테이블) → 관심 담기 → 비교
- [ ] **주변 분석**: 호재(재개발/재건축) 마커 + 학군 마커 표시
- [ ] **출퇴근**: 직장 검색 → 대중교통 소요시간 표시
- [ ] **비교**: 2개 단지 가격 추이 오버레이 차트
- [ ] **성능**: Lighthouse Performance 80+, API 응답 표시 p95 < 2초
- [ ] **반응형**: 모바일 360px ~ 데스크톱 1920px 정상 동작
- [ ] **배포**: Vercel 프로덕션 배포 완료

---

## 3. Phase 2: 분석 강화 (Sprint 5~6, 4주)

### Sprint 5: 전세/월세 + 추가 필터 (2주)

#### S5 진행 현황

| 항목 | 내용 |
|------|------|
| **완료율** | 100% — 단, 원래 스프린트 범위를 대폭 초과하여 **아키텍처 피벗(DB 사전 수집)** 을 이 스프린트에서 수행 |
| **소요 시간** | 약 3일 (피벗 작업 포함) |
| **블로커/리스크** | 실시간 API 방식에서 일 1,000건 호출 한도 초과 위험 현실화 → 배치 수집 + SQLite DB 방식으로 전환 불가피 |
| **의사결정** | ADR-002 번복: 실시간 API 호출 → DB 사전 수집으로 전환. `better-sqlite3` 도입, `scripts/collect.ts` 배치 수집 스크립트 작성, `/api/trade/search` Route Handler를 DB 쿼리 기반으로 전면 재작성. 전세/월세 기능은 DB 기반으로 자연스럽게 통합 |

#### S5-Goal
> 전세/월세 실거래가 분석, 전세가율 트렌드, 건축년도/세대수 필터 추가

| ID | 태스크 | 상세 설명 | 검증 방법 | PRD 매핑 |
|----|--------|-----------|-----------|----------|
| S5-01 | 아파트 전월세 API 연동 | data.go.kr `15126474` 연동 — Route Handler: `GET /api/trade/rent` | 강남구 전월세 데이터 반환 확인 | F3-3 |
| S5-02 | 거래 유형 탭 UI | 단지 상세 차트 영역에 [매매\|전세\|월세] 탭 추가, 탭 전환 시 차트 + 테이블 데이터 교체 | 매매→전세 탭 전환 시 차트 데이터 변경 | F3-3 |
| S5-03 | 전세가율 자동 계산 | 동일 단지/면적의 매매 평균가 대비 전세 평균가 비율 = `(전세가 / 매매가) × 100`, 월별 전세가율 추이 차트 | 전세가율 퍼센트 + 추이 차트 표시 | F3-5 |
| S5-04 | 갭투자 분석 지표 | 매매가 - 전세가 = 투자금, 투자금 추이 차트, 전세가율 60% 이상 하이라이트 | 투자금 수치 + 전세가율 배지 표시 | PRD 12 Phase 2 |
| S5-05 | 건축년도 범위 필터 | 시작년~종료년 드롭다운 2개, 조건 설정 페이지에 추가, URL 파라미터 연동 (`yearFrom`, `yearTo`) | 2015~2020 설정 → 해당 범위 단지만 표시 | F1-4 |
| S5-06 | 최소 세대수 필터 | 드롭다운: 전체, 100+, 300+, 500+, 1000+, 조건 설정 페이지에 추가, URL 파라미터 연동 (`minHousehold`) | 300+ 설정 → 300세대 미만 단지 제외 | F1-5 |
| S5-07 | 검색 결과 정렬 | 드롭다운: 최근거래가 높은순/낮은순, 면적 큰순/작은순, 건축년도 최신순/오래된순, 기본: 최근거래가 높은순 | 정렬 변경 시 리스트 순서 변경 + 지도 마커 유지 | F2-4 |
| S5-08 | 검색 결과 페이지네이션 | 20건/페이지, 페이지 번호 버튼 (최대 10페이지 표시 + ... 생략), 현재 페이지 하이라이트 | 42건 → 3페이지, 페이지 이동 시 리스트 변경 | F2-5 |
| S5-09 | 검색 결과 요약 바 | "총 42개 단지 \| 평균 거래가 6.1억 \| 평균 면적 71m²" 표시, 필터 변경 시 실시간 업데이트 | 요약 수치 정확성 확인 | F2-6 |

#### S5 완료 기준
- [ ] 매매/전세/월세 탭 전환 동작
- [ ] 전세가율 수치 + 추이 차트 표시
- [ ] 건축년도, 세대수 필터 적용 시 검색 결과 정확 필터링
- [ ] 정렬 + 페이지네이션 정상 동작

---

### Sprint 6: 추가 부동산 유형 + 비교 확장 + 학군 상세 (2주)

#### S6 진행 현황

| 항목 | 내용 |
|------|------|
| **완료율** | 100% — 원래 범위와 다르게 **사용자 인증(NextAuth v5 + 카카오 OAuth)** 과 **관심단지/비교분석 DB 영속화**를 이 스프린트에서 구현 |
| **소요 시간** | 약 2일 |
| **블로커/리스크** | 없음 |
| **의사결정** | 로드맵 S6 원안(연립다세대/오피스텔/학군 상세) 대신 사용자 가치가 높은 인증+즐겨찾기 DB 동기화 우선 구현으로 방향 전환; NextAuth v5 App Router 네이티브 지원 확인 후 채택; 비로그인 localStorage → 로그인 DB 자동 동기화 패턴 설계 |

#### S6-Goal
> 연립다세대/오피스텔 실거래가, 학군 상세 정보, 교통 호재 표시, 4개 단지 비교

| ID | 태스크 | 상세 설명 | 검증 방법 | PRD 매핑 |
|----|--------|-----------|-----------|----------|
| S6-01 | 연립다세대 매매 API 연동 | data.go.kr `15126467` 연동, 기존 아파트 프록시와 동일 패턴 | 연립다세대 검색 결과 반환 | F1-7 |
| S6-02 | 오피스텔 매매 API 연동 | data.go.kr `15126464` 연동 | 오피스텔 검색 결과 반환 | F1-7 |
| S6-03 | 유형별 통합 검색 | 부동산 유형 체크박스 활성화, 선택된 유형별 API 병렬 호출 → 결과 통합 정렬 | 아파트+오피스텔 동시 선택 시 통합 리스트 표시 | F1-7 |
| S6-04 | 학교알리미 상세 정보 연동 | data.go.kr `15014351` — 학생수, 학급수, 교원수 추가 표시 | 학교 카드에 학생수/학급수 표시 | F4-3 |
| S6-05 | 교통 호재 표시 | 신설 예정 지하철 노선/역 데이터 (JSON 정적 데이터 또는 API), 지도에 점선 노선 + 예정역 마커 | 주변 분석 [교통] 탭에 예정 노선 표시 | F4-2 |
| S6-06 | 비교 기능 확장 (4개) | 비교 페이지 URL: `/compare?ids=a,b,c,d`, 4개 단지 나란히 카드, 4개 라인 오버레이 차트, 비교 항목 추가: 전세가율, 출퇴근 시간 | 4개 단지 비교 차트 + 테이블 표시 | F6-2, F6-3 |
| S6-07 | 비교 항목 확장 | 비교 테이블에 추가: 전세가율, 최근 1년 가격 변동률(%), 출퇴근 시간, 최근 거래 건수 | 비교 테이블에 6개 이상 항목 비교 표시 | F6-3 |

#### S6 완료 기준
- [ ] 연립다세대, 오피스텔 검색 및 상세 조회 동작
- [ ] 학교 카드에 학생수/학급수 표시
- [ ] 4개 단지 비교 동작
- [ ] 비교 테이블에 전세가율, 가격변동률, 출퇴근 시간 포함

---

## 4. Phase 3: 입지 분석 고도화 (Sprint 7~8, 4주)

### Sprint 7: 등시선 + 자차 경로 + 편의시설 (2주)

#### S7 진행 현황

| 항목 | 내용 |
|------|------|
| **완료율** | 100% — 원안(등시선/자차/편의시설)과 다르게 **확장 필터 전면 구현, AI 인사이트, 3대 업무지구 접근성, 데이터 수집기 UI, 지도 성능 최적화** 수행 |
| **소요 시간** | 약 3일 |
| **블로커/리스크** | 없음 |
| **의사결정** | 등시선/자차 경로(ODsay/TMAP API 연동 비용·복잡도)보다 확장 필터(난방/복도/시공사/방수/승강기)와 AI 인사이트(Azure OpenAI)가 사용자 가치 높다 판단하여 우선 구현; 3대 업무지구(강남/여의도/광화문) 접근성 고정 카드 추가로 출퇴근 분석 UX 개선; `lib/collector/` 모듈 분리로 수집기-런타임 완전 분리 설계 확립 |

| ID | 태스크 | 상세 설명 | 검증 방법 | PRD 매핑 |
|----|--------|-----------|-----------|----------|
| S7-01 | 자차 출퇴근 경로 연동 | 카카오모빌리티 또는 TMAP 자동차 경로 API → 소요시간, 거리, 톨비 | 자차 소요시간 표시 | F5-3 |
| S7-02 | 등시선 지도 | ODsay 등시선 API → 직장 기준 30/60/90분 도달 범위 폴리곤 → 카카오맵 오버레이 | 30분 폴리곤 내 단지 하이라이트 | F5-4 |
| S7-03 | 편의시설 카테고리 검색 | 카카오 로컬 API 카테고리 검색 — 대형마트(MT1), 병원(HP8), 공원, 은행 → 마커 표시 | [편의시설] 탭에 카테고리별 마커 표시 | F4-4 |
| S7-04 | 인구 현황 표시 | data.go.kr `15108071` 법정동별 인구/세대 → 단지 상세에 동 단위 인구 정보 표시 | 해당 동 인구수, 세대수 표시 | F4-5 |
| S7-05 | 공시가격 대비 비율 | data.go.kr `3073746` 공동주택 공시가격 → 실거래가/공시가 비율 표시 | "실거래가/공시가 = 135%" 표시 | F3-6 |

### Sprint 8: 프리셋 + 층수 필터 + 비교 리포트 + 품질 (2주)

#### S8 진행 현황

| 항목 | 내용 |
|------|------|
| **완료율** | 100% (S8-01~06 + 추가 작업) |
| **소요 시간** | 약 3일 |
| **블로커/리스크** | Vercel Serverless 환경에서 `better-sqlite3` 바이너리 호환 불가 → `@libsql/client` (Turso) 로 드롭인 전환 필요 |
| **의사결정** | `better-sqlite3` → `@libsql/client` 마이그레이션 (v2.4 피벗): Vercel 파일시스템 읽기 전용 제약으로 SQLite 파일 직접 사용 불가, Turso 원격 DB 전환; 로컬 개발은 `file:real-estate.db` URL 유지로 개발 편의성 보존; 가이드 UI(`/dev`) 추가로 수집기 브라우저 조작 가능; 테스트 환경(Jest) 구축 및 `lib/format.ts` 단위 테스트 작성 |

| ID | 태스크 | 상세 설명 | 검증 방법 | PRD 매핑 |
|----|--------|-----------|-----------|----------|
| S8-01 | 조건 프리셋 저장/불러오기 | localStorage에 필터 조건 세트 저장 (이름 지정), 최대 5개, 메인 페이지에 프리셋 드롭다운 | 프리셋 저장 → 불러오기 시 필터 복원 | F1-8 |
| S8-02 | 층수 범위 필터 | 저층(1~5)/중층(6~15)/고층(16+) 체크박스, 거래 데이터의 층 필드 기반 필터링 | 고층만 선택 → 16층+ 거래만 표시 | F1-6 |
| S8-03 | 비교 결과 이미지 내보내기 | html2canvas로 비교 뷰 캡처 → PNG 다운로드 | "이미지 저장" 클릭 → PNG 파일 다운로드 | F6-4 |
| S8-04 | 비교 결과 URL 공유 | 비교 페이지 URL에 모든 상태 포함 (단지 ID, 직장 좌표), "링크 복사" 버튼 | 링크 공유 → 동일 비교 화면 표시 | F6-4 |
| S8-05 | 성능 최적화 | React.memo, useMemo 적용, 번들 분석 (next/bundle-analyzer), 이미지 lazy loading, API 요청 deduplication | Lighthouse Performance 85+ | NF-1 |
| S8-06 | 접근성 개선 | 키보드 네비게이션, aria-label, 스크린리더 테스트, 색상 대비 확인 | WAVE 접근성 도구 에러 0건 | NF-7 |

---

## 5. Phase 4~6: 장기 로드맵 (Sprint 9+)

### Phase 4: AI 추천 (Sprint 9~10)

| 기능 | 설명 |
|------|------|
| 사용자 선호도 프로파일링 | 검색/조회 이력 기반 선호 지역, 가격대, 면적 패턴 분석 |
| AI 단지 추천 | 조건 + 선호도 기반 상위 10개 단지 추천, 추천 이유 설명 |
| 유사 단지 추천 | "이 단지와 비슷한 단지" — 가격대, 면적, 지역, 건축년도 유사도 기반 |

### Phase 5: 사용자 계정 + 알림 (Sprint 11~12)

| 기능 | 설명 |
|------|------|
| 사용자 인증 | NextAuth.js 기반 소셜 로그인 (카카오, 구글) |
| 클라우드 관심 목록 | DB 기반 관심 단지 저장 (기기 간 동기화) |
| 가격 변동 알림 | 관심 단지의 새 거래 등록 시 이메일/푸시 알림 |
| 조건 알림 | 사용자 조건에 맞는 새 거래 발생 시 알림 |

### Phase 6: 모바일 앱 (Sprint 13+)

| 기능 | 설명 |
|------|------|
| PWA 전환 | Service Worker, 오프라인 캐시, 홈 화면 추가 |
| 또는 React Native | 네이티브 앱 (iOS/Android), 푸시 알림 네이티브 지원 |

---

## 6. 기술적 의사결정 기록 (ADR 요약)

### ADR-001: 인메모리 캐시 vs Redis

| 항목 | 결정 |
|------|------|
| **결정** | MVP는 `Map` 기반 인메모리 캐시, Phase 2부터 Redis 전환 검토 |
| **이유** | Vercel Serverless 환경에서 Redis는 외부 서비스 의존 (Upstash 등), MVP 복잡도 최소화 |
| **위험** | Serverless cold start 시 캐시 초기화 → 공공데이터 API 호출량 증가 |
| **전환 기준** | 일일 API 호출 5,000건 초과 시 Redis(Upstash) 도입 |

### ADR-002: 실거래가 데이터 수집 전략

| 항목 | 결정 |
|------|------|
| **결정** | 실시간 API 호출 + 인메모리 캐시 (24시간 TTL) |
| **대안** | 야간 배치 크론잡으로 DB에 사전 수집 |
| **이유** | MVP에서는 DB 없이 시작, 사용자 요청 시점에 API 호출이 더 단순 |
| **전환 기준** | API 호출 한도 초과 또는 응답 지연 p95 > 3초 발생 시 배치 수집 전환 |

### ADR-003: 법정동코드 ↔ 단지 매핑

| 항목 | 결정 |
|------|------|
| **결정** | 실거래가 API의 단지명 + 법정동 기반 그룹핑 (정확 매칭) |
| **문제** | 같은 단지도 "래미안퍼스티지" vs "래미안 퍼스티지" 등 표기 불일치 가능 |
| **완화** | 공백/특수문자 제거 후 비교, 단지 목록 API(`15057332`) 단지코드를 정규 키로 사용 |

---

## 7. API 호출 예산 계획

### 일일 API 호출 예상 (MVP, MAU 1,000명 기준)

| API | 호출/사용자/세션 | 일일 예상 | 한도 | 여유율 |
|-----|------------------|-----------|------|--------|
| 아파트 매매 실거래가 | 12건 (12개월 조회) | 4,000건 | 10,000건 | 60% |
| 단지 목록 | 2건 | 700건 | 10,000건 | 93% |
| 단지 기본정보 | 3건 | 1,000건 | 10,000건 | 90% |
| 카카오 로컬 | 5건 | 1,700건 | 300,000건 | 99% |
| ODsay 대중교통 | 2건 | 700건 | 무료 기본 | - |

> **가정**: DAU = MAU × 0.33 = 330명, 평균 세션 1회/일
> **캐시 효과**: 동일 법정동+월 조회 시 캐시 히트 → 실제 API 호출 약 40% 절감

---

## 8. 스프린트 검증 체크리스트 템플릿

각 스프린트 종료 시 아래 체크리스트로 검증:

```markdown
## Sprint [N] 검증 체크리스트

### 기능 검증
- [ ] 모든 태스크 산출물 존재 확인
- [ ] 각 태스크의 "검증 방법" 통과
- [ ] 신규 API 엔드포인트 curl 테스트 통과

### 품질 검증
- [ ] `npm run build` 에러 0건
- [ ] TypeScript 타입 에러 0건
- [ ] ESLint 경고 5건 이하
- [ ] 콘솔 에러 0건 (브라우저 DevTools)

### 성능 검증
- [ ] Lighthouse Performance: [목표점수]+
- [ ] API 응답 표시 p95 < [목표초]초
- [ ] 번들 사이즈 증가 < 50KB (이전 스프린트 대비)

### UX 검증
- [ ] 모바일 360px 레이아웃 정상
- [ ] 데스크톱 1920px 레이아웃 정상
- [ ] 로딩 상태 표시 확인
- [ ] 에러 상태 표시 확인
- [ ] 빈 결과 상태 표시 확인

### 배포 검증
- [ ] Vercel Preview 배포 정상
- [ ] 환경변수 설정 확인
- [ ] 프로덕션 빌드 테스트
```

---

## 9. 의존성 및 선행 조건

### 프로젝트 시작 전 필수 준비 (Sprint 1 이전)

| 항목 | 설명 | 소요 시간 | 담당 |
|------|------|-----------|------|
| data.go.kr API 키 발급 | 실거래가(15126469), 단지목록(15057332), 단지정보(15058453), 학교(15122275) — 총 4개 API 활용신청 | 즉시~2일 | 개발자 |
| 카카오 개발자 앱 등록 | REST API 키 + JS SDK 앱 키 발급, 카카오맵 사용 설정 활성화 | 즉시 | 개발자 |
| ODsay API 키 발급 | lab.odsay.com 회원가입 + 키 발급 | 즉시 | 개발자 |
| 서울열린데이터 API 키 | 재개발/재건축 정비사업 API 활용신청 | 즉시~1일 | 개발자 |
| Vercel 계정 | GitHub 연동, 프로젝트 생성 | 즉시 | 개발자 |
| 법정동코드 파일 | code.go.kr에서 다운로드 + JSON 변환 | 2시간 | 개발자 |

### 스프린트 간 의존성

```
S1 (부트스트랩 + API)
 ├──→ S2 (조건설정 + 검색결과)    ※ S1의 API 프록시, 법정동코드 필요
 │     └──→ S3 (상세 + 차트)      ※ S2의 검색→상세 라우팅 필요
 │           └──→ S4 (호재 + 비교) ※ S3의 상세 페이지 레이아웃 필요
 │                 └──→ MVP 출시
 └──→ S5 (전세/월세 + 필터)        ※ S1의 API 패턴 재사용
       └──→ S6 (유형 확장 + 비교)
             └──→ S7 (등시선 + 편의시설)
                   └──→ S8 (프리셋 + 품질)
```

---

## 10. 릴리즈 마일스톤

| 마일스톤 | 시점 | 주요 기능 | 검증 기준 |
|----------|------|-----------|-----------|
| **Alpha** | Sprint 2 종료 | 조건 설정 → 검색 결과 (지도+리스트) | 강남구 아파트 검색 → 10개+ 단지 표시 |
| **Beta** | Sprint 3 종료 | + 단지 상세 (차트+테이블) + 출퇴근 | 전체 플로우: 검색→상세→출퇴근 조회 |
| **MVP (v1.0)** | Sprint 4 종료 | + 호재 + 관심목록 + 비교 | PRD Section 9 MVP 범위 전체 충족 |
| **v1.1** | Sprint 6 종료 | + 전세/월세 + 추가 유형 + 4개 비교 | 전세가율 표시, 연립/오피스텔 검색 |
| **v1.2** | Sprint 8 종료 | + 등시선 + 편의시설 + 공시가 비율 | 등시선 지도 표시, 편의시설 마커 |
| **v2.0** | Sprint 10 종료 | + AI 추천 | 추천 단지 10개 리스트 표시 |
