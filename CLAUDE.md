# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

## 프로젝트 개요

**부동산 매수 도우미 포털** — 대한민국 공공데이터(data.go.kr)를 활용하여 부동산 매수 희망자가 조건별 매물 탐색, 실거래가 추이 분석, 주변 호재/학군/출퇴근 분석, 단지 비교를 한 곳에서 할 수 있는 웹 포털.

- 기획 문서: `PRD.md` (요구사항 정의), `RoadMap.md` (스프린트 계획)
- 현재 상태: 기획 완료, 구현 전 (Sprint 1 시작 전)
- 스프린트 태스크 ID(S1-01~S8-06)와 PRD 기능 ID(F1-1~F6-4)를 참조하여 요구사항 추적성을 유지한다.

## 기술 스택

| 계층 | 기술 |
|------|------|
| Frontend | Next.js 14+ (App Router, TypeScript) |
| UI | shadcn/ui + Tailwind CSS |
| 차트 | Recharts |
| 지도 | 카카오맵 JS SDK |
| Backend | Next.js Route Handlers (API 프록시) |
| 캐시 | 인메모리 Map (MVP), Redis 전환 예정 |
| 배포 | Vercel |

## 빌드 및 실행 명령어

```bash
npm run dev          # 개발 서버 (localhost:3000)
npm run build        # 프로덕션 빌드
npm run lint         # ESLint 실행
npm test             # 전체 테스트
```

## 아키텍처

```
브라우저 → Next.js Frontend (App Router)
              ↓
         Route Handlers (API 프록시, 서비스키 은닉)
              ↓
         ┌─ data.go.kr (실거래가, 단지정보, 학교, 인구)
         ├─ 카카오 로컬 API (주소 검색, 장소 검색)
         ├─ ODsay API (대중교통 경로)
         └─ 서울열린데이터 (재개발/재건축)
              ↓
         인메모리 캐시 (실거래가 24h, 단지정보 7d, 학교/인구 30d)
```

- **모든 외부 API 호출은 Route Handler를 경유**한다 — 클라이언트에 API 키 노출 금지
- 환경변수: `DATA_GO_KR_API_KEY`, `KAKAO_REST_API_KEY`, `KAKAO_JS_APP_KEY`, `ODSAY_API_KEY`
- 환경변수에 `NEXT_PUBLIC_` 접두사를 사용하지 않는다 (카카오 JS SDK 앱 키 제외)

## 핵심 공공데이터 API

| API | data.go.kr ID | 파라미터 |
|-----|---------------|----------|
| 아파트 매매 실거래가 | `15126469` | `LAWD_CD`(법정동5자리) + `DEAL_YMD`(YYYYMM) |
| 아파트 전월세 | `15126474` | 동일 |
| 단지 목록 | `15057332` | 법정동코드 |
| 단지 기본정보 | `15058453` | 단지코드 |
| 학교 기본정보 (NEIS) | `15122275` | 시도교육청코드 + 학교명 |

- 응답 기본 형식은 XML이며, `_type=json` 파라미터로 JSON 변환
- 법정동코드 데이터: `data/region-codes.json` (시/도 → 시/군/구 → 읍/면/동 계층)

## 금액/면적 포맷팅

- 금액: "5억", "5억 2,000만" 형식 (`lib/format.ts`)
- 면적: m² 기본, 평 전환 지원 (1평 = 3.3058m²)

---

## 1. 문제 정의 및 문서화

- 모든 기능 구현 전에 **문제 정의 → 목표 → 기능 명세** 순서로 문서화한다.
- `docs/` 디렉토리에 다음 문서를 유지한다:
  - `docs/PRD.md` — 프로젝트 정의서 (문제 정의, 목표, 범위)
  - `docs/FEATURES.md` — 기능 명세 (기능별 상세 스펙, 우선순위, 상태)
  - `docs/ARCHITECTURE.md` — 아키텍처 문서 (구조, 데이터 흐름, 의존성)
  - `docs/TECH_STACK.md` — 기술 스택 선정 근거
  - `docs/CHANGELOG.md` — 변경 이력
- 기능 추가/변경 시 관련 문서를 **코드보다 먼저** 업데이트한다.
- 문서는 한국어로 작성한다.

## 2. Git 이력 관리

- 커밋 메시지는 다음 형식을 따른다:
  ```
  <type>(<scope>): <subject>

  <body — 변경 이유와 상세 내용>

  <footer — breaking changes, 관련 이슈 등>
  ```
- **type**: `feat`, `fix`, `docs`, `style`, `refactor`, `test`, `chore`
- 커밋은 하나의 논리적 변경 단위로 만든다 (atomic commit).
- `docs/CHANGELOG.md`에 주요 변경사항을 날짜별로 기록한다.
- Push 전 변경사항 요약을 커밋 본문에 상세히 남긴다.

## 3. 클린 코드 & 유지보수성

- **간결한 구조**: 함수는 하나의 역할만 수행한다 (SRP).
- **중복 제거**: 같은 로직이 2회 이상 반복되면 함수/모듈로 추출한다. 단, 1회 사용은 추상화하지 않는다.
- **네이밍**: 의미를 명확히 드러내는 이름을 사용한다. 축약어를 피한다.
- **파일 구조**: 관심사별로 분리한다 (HTML/CSS/JS, 컴포넌트 단위).
- **코드 리뷰**: 구현 후 구조적 문제가 없는지 스스로 점검한다.
  - 순환 의존성, 과도한 결합, 불필요한 복잡도를 확인한다.

## 4. 코드 가독성 & 오류 처리

- **일관성**: 들여쓰기 2칸, 세미콜론 사용, 작은따옴표 통일.
- **포맷팅**: Prettier 설정을 프로젝트 루트에 유지한다.
- **오류 처리 원칙**:
  - 외부 입력(사용자 입력, API 응답)은 반드시 검증한다.
  - 내부 코드 간 호출은 타입/계약을 신뢰한다 — 방어적 코드를 남발하지 않는다.
  - 에러 메시지는 **원인 + 맥락**을 포함한다 (예: `"Failed to load config: file not found at /path"`).
  - try-catch는 복구 가능한 지점에서만 사용한다. 복구 불가능하면 상위로 전파한다.

## 5. 기술 스택 선정

- 기술 선택 시 **근거를 반드시 문서화**한다 (`docs/TECH_STACK.md`).
- 근거에 포함할 항목:
  - 요구사항과의 적합성
  - 대안 기술과의 비교 (최소 1개)
  - 커뮤니티/생태계 성숙도
  - 학습 곡선 및 팀 역량 고려
- 과도한 기술 도입을 피한다. 요구사항에 맞는 최소한의 스택을 선택한다.

## 6. UI/UX & 반응형 디자인

- **직관적 UI**: 사용자가 설명 없이 사용할 수 있어야 한다.
- **반응형 브레이크포인트**:
  - Mobile: `< 768px`
  - Tablet: `768px ~ 1024px`
  - Desktop: `> 1024px`
- **모바일 퍼스트**: 기본 스타일은 모바일 기준, 미디어 쿼리로 확장한다.
- 터치 대상은 최소 `44px × 44px`을 유지한다.
- 색상 대비는 WCAG AA 기준(4.5:1)을 충족한다.
- 애니메이션은 `prefers-reduced-motion`을 존중한다.

## 7. 테스트 환경

- 프로젝트에 테스트 실행 환경을 구축하고 유지한다.
- **단위 테스트**: 개별 함수/모듈의 동작을 검증한다.
- **통합 테스트**: 모듈 간 상호작용을 검증한다.
- **UI 테스트**: 주요 사용자 시나리오를 검증한다.
- 기능 추가/변경 후 관련 테스트를 반드시 작성하거나 업데이트한다.
- 테스트 실행 방법을 `README.md` 또는 `docs/`에 명시한다.
- `npm test`로 전체 테스트를 실행할 수 있어야 한다.

---

## 작업 흐름 요약

```
1. 요구사항 확인 → 문서화 (PRD, FEATURES)
2. 기술 스택 검토 → 근거 기록 (TECH_STACK)
3. 아키텍처 설계 → 문서화 (ARCHITECTURE)
4. 구현 → 클린 코드 원칙 준수
5. 테스트 작성 → 동작 검증
6. 코드 리뷰 → 구조/가독성 점검
7. 커밋 → Conventional Commits, 상세 본문
8. CHANGELOG 업데이트
```
